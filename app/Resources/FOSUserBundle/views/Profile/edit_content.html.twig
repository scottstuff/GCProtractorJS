<form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST"
      class="fos_user_profile_edit form-horizontal">

    {{ form_row(form.username) }}
    {{ form_row(form.email) }}

    <div class="control-group">
        <div class="controls">
            <a href="{{ path('fos_user_change_password') }}">Change your Password</a>
        </div>
    </div>

    {#
    <fieldset>
        <legend>Profile</legend>

        {{ form_row(form.fullName) }}
        {{ form_row(form.rescueName) }}
        {{ form_row(form.address) }}
        {{ form_row(form.addressLine2) }}
        {{ form_row(form.city) }}
        {{ form_row(form.state) }}
        {{ form_row(form.zipCode) }}
        {{ form_row(form.phone) }}
    </fieldset>

    <fieldset>
        <legend>Import Service Configuration</legend>
        {{ form_row(form.preferences.adoptapet_username,
            {'help_inline' : '<a href="' ~ path('importer_adoptapet') ~ '">Import Data</a>'}) }}
        {{ form_row(form.preferences.adoptapet_password) }}
        {{ form_row(form.preferences.petfinder_username,
            {'help_inline' : '<a href="' ~ path('importer_petfinder') ~ '">Import Data</a>'}) }}
        {{ form_row(form.preferences.petfinder_password) }}
    </fieldset>
    #}

    <fieldset>
        <legend>&nbsp;</legend>
        {{ form_row(form.current_password, {'label' : 'Verify your Password', 'help_block' : 'Please verify your password in order to make changes.'}) }}
    </fieldset>

    {{ form_rest(form) }}

    <div class="form-actions">
        <input type="submit" class="btn btn-primary" value="{{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>
